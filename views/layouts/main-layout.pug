<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title #{pageTitle}
        link(rel="stylesheet", href="/css/main.css")
        block styles
    body   
        header.main-header
            nav.main-header__nav
                ul.main-header__item-list
                    li.main-header__item
                        a(href="/", class=(path === '/' ? 'active' : '')) Shop
                    li.main-header__item
                        a(href="/admin/add-product", class=(path === '/admin/add-product' ? 'active' : '')) Add Product
        block content


//-  cr8 basic layout with two hooks, we can dynamically enter content into from inside our

//-  with adding block styles, we able to add mode styles from inside other files here

//-  adding block content

//- definition : a(href="/admin/add-product", class=(path === '/admin/add-product' ? 'active' : '')) Add Product:
//- add check if path equal value ('/admin/add-product') then render active as class here otherwise an empty string

//- add check path for passing path from shop.js / matching path shop at shop.js with main-layout.pug

//-  add dynamic output on title = title #{pageTitle}